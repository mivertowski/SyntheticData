{{- if .Values.config.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "datasynth.fullname" . }}-config
  labels:
    {{- include "datasynth.labels" . | nindent 4 }}
data:
  datasynth.yaml: |
    {{- .Values.config.content | nindent 4 }}
{{- end }}
